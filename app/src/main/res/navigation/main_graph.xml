<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_graph"
    app:startDestination="@id/recipeListFragment">

    <fragment
        android:id="@+id/recipeListFragment"
        android:name="com.example.recapp.RecipeListFragment"
        android:label="RecipeListFragment" >

        <action
            android:id="@+id/viewRecipe"
            app:destination="@id/receipeFragment" />
    </fragment>
    <fragment
        android:id="@+id/receipeFragment"
        android:name="com.example.recapp.presentation.ui.recipe.ReceipeFragment"
        android:label="ReceipeFragment" />
</navigation>